# Lumina Blog Platform

一个极简、科技感、响应式的博客平台。

## 🚀 如何部署 (Deploy)

本项目已经配置好，可以一键部署到 Vercel。

### 方法 A: 极速部署 (推荐)

如果你将代码下载到了本地：

1.  注册/登录 [Vercel.com](https://vercel.com)。
2.  点击 **"Add New..."** -> **"Project"**。
3.  导入你的 Git 仓库（或者直接上传本地文件夹 CLI）。
4.  **Framework Preset** 选择 `Vite`。
5.  点击 **Deploy**。

Vercel 会自动识别 `package.json` 并运行构建命令。

### 方法 B: 本地开发

1.  安装依赖：
    ```bash
    npm install
    ```
2.  启动开发服务器：
    ```bash
    npm run dev
    ```
3.  构建生产版本：
    ```bash
    npm run build
    ```

## 🛠️ 项目结构

*   `vite.config.ts`: 构建配置
*   `tailwind.config.js`: 样式系统配置
*   `vercel.json`: 解决单页应用 (SPA) 路由刷新 404 问题

## 🔑 账号信息

*   登录地址: `/login`
*   密码: `admin123`
